in the git bash, write:
# Move into your local folder:
cd E:/backup/Data/Learning/Kaggle/SQL

#Initialize Git inside it:
git init

#Mark the Folder as Safe
git config --global --add safe.directory E:/backup/Data/Learning/Kaggle/SQL

#Link it to your GitHub repository:
git remote add origin https://github.com/MMeybodi/SQL_Kaggle

#Check if it's linked correctly:
git remote -v



git config --global user.name "Morteza Meybodi"
git config --global user.email "morteza.meybodi@yahoo.com"

#Add, commit
git add .
git commit -m "Initial commit"

# Check which branch you're on in your local repo:
git branch

#If it says master, it means your local branch is called master.
#If it says main, your local branch is called main

#push if master
git push -u origin master
#push if main
git push -u origin main

-----------------------------------------------


git add SQL_Kaggle.ipynb
git add stores.csv
git add features.csv
git add train.csv
git add london_merged.csv
git add hotel_bookings.csv
git commit -m "changed directory"
git push -u origin master
